import 'package:sakan/features/auth/sign_up/models/profile_model.dart';

// Re-exporting or copying the model to follow architecture
// For this project, we'll use the existing one but if we want it self-contained:
export 'package:sakan/features/auth/sign_up/models/profile_model.dart';
